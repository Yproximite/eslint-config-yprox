// eslint-disable-next-line
import Vue from 'vue';

import SiteIcon from './components/SiteIcon.vue';

Vue.component('SiteIcon', SiteIcon);

const app = new Vue({
  el: '#app',
});

console.log(app);
